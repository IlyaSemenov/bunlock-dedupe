---
"bunlock-dedupe": minor
---

Honor dependency overrides stored in `bun.lock` during deduplication, update analysis, and safety checks, while showing both declared and overridden ranges in reports.
